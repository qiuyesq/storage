mod pkg;

use rand;

// use crate::pkg::proto::configuration;

fn main() {
    println!("Code for the distbuild storage!");
    let a = rand::random::<u8>();
    print!("{}",a);

    // let configuration = configuration::storage::ApplicationConfiguration;
}
